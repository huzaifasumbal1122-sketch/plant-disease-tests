{
  "name": "plant-disease-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha *.js --timeout 30000"
  },
  "dependencies": {
    "selenium-webdriver": "^4.0.0",
    "mocha": "^10.0.0",
    "assert": "^2.0.0"
  }
}
